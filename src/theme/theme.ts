import {ITheme} from './entities';
import {darkContent} from './utils/dark-content';
import {lightContent} from './utils/light-content';

export const theme: ITheme = {
  dark: darkContent,
  light: lightContent,
};
